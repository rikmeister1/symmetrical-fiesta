Run the `chaos` skill.

The skill executes the divergent chaos protocol: a deliberate cognitive jolt that emits heterogeneous fragments (obscure facts, physics, poetry, multilingual shards, silence, non-sequiturs, strange adjacencies) in waves, then maps the resulting raw associations back to the problem as fresh seed ideas. Originally Step 2.5 of Phase 2 (ideation), now also exposed as a standalone slash command for use when divergence has collapsed in any phase or outside any phase.

Skill definition: `.claude/skills/chaos/SKILL.md`. Protocol specification: `.claude/docs/chaos_protocol.md`. Phase 2 integration: `.claude/docs/phase_2_ideation.md` (Step 2.5).

Additional context from the user: $ARGUMENTS
