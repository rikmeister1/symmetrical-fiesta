Run the `innovate` skill.

The skill executes the Vibe Innovation Framework entry protocol: language selection, team identity, ICD availability check, entry diagnostic, TRL mapping, session plan, ICD initialization, and phase dispatch.

Skill definition: `.claude/skills/innovate/SKILL.md`. Process specification: `.claude/docs/orchestrator.md`. ICD specification: `.claude/docs/innovation_canvas_document.md`. TRL scale: `.claude/docs/trl_specification.md`.

Additional context from the user: $ARGUMENTS
